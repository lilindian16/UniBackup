def abs_values(numbers):
    """This will return the absoulte value for each number inside a parameter
    """
    result = []
    for num in numbers:
        result.append(abs(num))
    return result
    
    