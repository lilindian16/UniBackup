def cubed_tuple(number):
    """This will take a number as a parameter and cube it and present it in the 
    wanted format
    """
    result = (number * number * number)
    return("{0}, {1}".format(number, result)